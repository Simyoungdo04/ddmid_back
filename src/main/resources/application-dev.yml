spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: oracle.jdbc.OracleDriver

kakao:
  rest-api-key: ${KAKAO_REST_API_KEY}

tmap:
  app-key: ${TMAP_APP_KEY}

logging:
  level:
    com.kh.midpoint: DEBUG
