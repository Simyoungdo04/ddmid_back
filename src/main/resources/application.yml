spring:
  application:
    name: midpoint-api
  profiles:
    active: local          # 기본값. 서버에서는 SPRING_PROFILES_ACTIVE 환경변수로 덮어씀
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
